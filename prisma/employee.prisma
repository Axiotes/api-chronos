model Employee {
  id           Int              @id @default(autoincrement())
  name         String           
  cpf          String           @unique
  arrivalTime  String
  exitTime     String
  createdAt    DateTime         @default(now())
  updatedAt    DateTime?

  timeRecords TimeRecords[]
  Cards Cards[]
}